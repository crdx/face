# this is bad - figure out a better way

class Wrapper():
    def __init__(self, face, connection, event, config, util):
      self.face = face
      self.connection = connection
      self.event = event
      self.config = config
      self.util = util
